ansible modules as all via roles

To ensure this works, you can configure
ansible to have no modules in its module
paths

for ex, for running out of source,
mv lib/ansible/modules lib/ansible/modules_not


